import React from 'react';
import {connect} from 'react-redux';
import '../images/FaceUpDeck.svg'

export function dealerTable() {

  return (
    <div id="container">
      
    </div>
  )
}

export default connect()(dealerTable);